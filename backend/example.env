# --- CORS ---
# Production: Set to your deployed frontend URL (e.g., "https://app.yourdomain.com")
# Development: Use "*" to allow all origins (ngrok, localhost, Vercel preview URLs)
ALLOWED_ORIGINS=["*"]
ALLOWED_METHODS=["*"]
ALLOWED_HEADERS=["*"]
ALLOW_CREDENTIALS=true
CORS_MAX_AGE=3600
